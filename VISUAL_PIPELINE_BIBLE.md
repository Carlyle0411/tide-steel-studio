# VISUAL PIPELINE BIBLE

项目：《潮汐钢魂》  
用途：全系列视觉资产生产流水线  
版本：V1

---

# 01 核心原则

所有视觉资产必须像电影工业资产一样管理。

角色、机甲、生物、场景、道具都不能只停留在“好看的图”。  
每一张图必须有来源、版本、审核状态和可用范围。

没有Approved的图片，禁止进入Kling。  
没有Approved的图片，禁止进入Veo。  
没有登记进资产库的图片，禁止进入Storyboard。

---

# 02 标准流程

Reference  
↓  
GPT Image2  
↓  
人工审核  
↓  
Approved  
↓  
资产库  
↓  
Storyboard  
↓  
Video

任何资产都不能跳过人工审核。

---

# 03 目录规则

每个资产目录必须包含：

- `README.md`
- `REFERENCE.md`
- `CONSISTENCY_RULES.md`
- `IMAGE_RULES.md`
- `PROMPT_LIBRARY.md`
- `ASSET_INDEX.md`
- `draft/`
- `review/`
- `approved/`
- `deprecated/`

## draft

用于存放初步生成图、探索图、未审核变体。

规则：

- 可以失败。
- 可以重复。
- 可以偏离。
- 禁止进入视频。

## review

用于存放等待人工审核的候选资产。

规则：

- 必须来自draft。
- 必须具备生成说明。
- 必须说明使用Reference。
- 禁止进入视频。

## approved

用于存放已通过人工审核的锁定资产。

规则：

- 可以进入Storyboard。
- 可以进入Kling。
- 可以进入Veo。
- 可以作为后续GPT Image2参考。
- 不可随意替换。

## deprecated

用于存放废弃资产和历史版本。

规则：

- 不得用于新生产。
- 保留原因必须写入`ASSET_INDEX.md`。
- 不直接删除，避免历史生产链断裂。

---

# 04 新图片生产规则

任何新图片生成前必须完成：

1. 查询`ASSET_DATABASE.md`确认资产编号。
2. 读取对应资产目录的`REFERENCE.md`。
3. 读取对应资产目录的`CONSISTENCY_RULES.md`。
4. 读取对应资产目录的`IMAGE_RULES.md`。
5. 确认该资产是否允许在当前集数出现。
6. 确认是否存在锁定Reference。

生成后必须完成：

1. 保存至对应资产目录的`draft/`。
2. 写入生成记录。
3. 人工审核。
4. 通过后复制到`approved/`。
5. 更新`ASSET_INDEX.md`。
6. 更新`ASSET_DATABASE.md`。

---

# 05 Kling / Veo准入规则

进入Kling或Veo前，必须满足：

- 图片位于`approved/`。
- `ASSET_DATABASE.md`中“是否Approved”为Yes。
- `ASSET_DATABASE.md`中“是否可以用于Kling”或“是否可以用于Veo”为Yes。
- 资产版本号明确。
- Reference来源明确。
- 与当前剧情阶段不冲突。

禁止：

- 使用draft图直接生成视频。
- 使用review图直接生成视频。
- 使用未登记图生成视频。
- 使用Deprecated资产生成新镜头。
- 为了临时好看绕过资产库。

---

# 06 GPT Image2准入规则

进入GPT Image2生成前，必须明确：

- 生成的是哪个资产。
- 使用哪些Reference。
- 属于哪个集数。
- 属于角色、机甲、生物、场景还是道具。
- 是否会破坏已Approved资产一致性。

若当前工具无法读取Reference image input，必须在审核记录中说明，并把结果标记为“文本绑定Reference生成”，不得伪装为图像输入一致性。

---

# 07 资产编号规则

编号格式：

- 角色：`CHAR-NAME-001`
- 机甲：`MECHA-NAME-001`
- 生物：`CRE-NAME-001`
- 场景：`ENV-NAME-001`
- 道具：`PROP-NAME-001`

版本格式：

- 初始未定稿：`V0`
- 第一版定稿：`V1`
- 微调：`V1.1`
- 重大重设：`V2`

版本升级必须记录原因。

---

# 08 一致性红线

角色：

- 同一人物不能换脸。
- 服装系统不能随机变化。
- 年龄感不能漂移。

机甲：

- 结构比例不能随机变化。
- 赤霆01必须保持暗红装甲、黑色骨架、蓝色同步光。
- 未到剧情阶段禁止提前展示完整机甲。

生物：

- 白潮不能怪兽王化。
- 黑潮母体不能Boss化。
- 潮门生物系统不能变成游戏副本入口。

场景：

- 2047杭州湾必须保持未来工业、海防工程、湿冷空气。
- 深蓝基地不能变成干净科幻大厅。
- 海洋永远比城市更大。

道具：

- 必须来自同一工业设计体系。
- 不能像玩具。
- 不能出现无来源发光装饰。

---

# 09 审核问题清单

每张图进入Approved前，必须回答：

1. 它属于哪个资产编号？
2. 它使用了哪些Reference？
3. 它是否符合当前集数允许展示的程度？
4. 它是否保持角色、机甲、生物、场景或道具一致性？
5. 它是否像真实电影画面，而不是游戏CG？
6. 它是否违反任何导演圣经规则？
7. 它是否可以进入Storyboard？
8. 它是否可以进入Kling？
9. 它是否可以进入Veo？
10. 它是否需要进入Deprecated替换旧版本？

---

# 10 生产纪律

视觉生产不是不断生成更多图片。

视觉生产是让每一张可以被未来使用的图片都拥有身份、来源、规则和责任。

《潮汐钢魂》的资产库必须比单张图片更重要。  
只有这样，36集连续章节式AI科幻电影系列才不会在视觉上散掉。
